/*
 * @flow 
 * {{ properCase name }} actions
 */
import {
  DEFAULT_ACTION
} from './actions-type';

// type Dispatch = (dispatcher: Object)=>void

export function defaultAction () {
  return {
    type: DEFAULT_ACTION
  };
}
